name: fall_down
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

  sensors: ^0.4.2+2
  box2d_flame: ^0.4.6
  url_launcher: ^5.5.0
  flame_splash_screen: ^0.0.1
  shared_preferences: ^0.5.8

  supercharged: ^1.7.0
  flutter_lints: ^1.0.4

  flame:
    git:
      url: https://github.com/idootop/flame.git
      ref: patch_0.24.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/images/