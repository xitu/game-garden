export class GameObject {
  constructor (x, y) {
    this.x = x
    this.y = y
    this.visible = true
  }

  update () {}

  // eslint-disable-next-line no-unused-vars
  draw (renderer) {}
}
